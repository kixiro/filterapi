#!/bin/bash

rm -f filterapi
rm -rf build/
rm -rf dist/
cp ../../filterapi .
docker build -t filterapi_debian8 .
rm filterapi
docker run -it -v `pwd`:/root/mount/ filterapi_debian8 cp /root/src/dist/filterapi /root/mount
